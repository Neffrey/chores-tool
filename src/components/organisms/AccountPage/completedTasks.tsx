// FC
const CompletedTasks = () => {
  return (
    <div>
      Completed Tasks
      <button
      //onClick={() => console.log({ userData })}
      >
        Log UserData
      </button>
    </div>
  );
};

export default CompletedTasks;
